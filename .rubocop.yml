plugins:
  - rubocop-rails
  - rubocop-factory_bot
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 3.2
  NewCops: enable
  SuggestExtensions: false
  Exclude:
    - "vendor/**/*"

Style/Documentation:
  Enabled: false

Style/ClassAndModuleChildren:
  EnforcedStyle: compact

Metrics/BlockLength:
  Exclude:
    - "spec/**/*"
    - "*.gemspec"
    - "lib/poly/joins.rb"

Metrics/AbcSize:
  Exclude:
    - "lib/poly/joins.rb"
    - "lib/poly/stack.rb"

Metrics/MethodLength:
  Exclude:
    - "lib/poly/joins.rb"
    - "lib/poly/stack.rb"

Metrics/ParameterLists:
  Max: 6

# Poly::Stack deliberately demotes the prior prime and links the audit edge
# without re-running validations/callbacks on the superseded card.
Rails/SkipsModelValidations:
  Exclude:
    - "lib/poly/stack.rb"

Rails/RakeEnvironment:
  Enabled: false

RSpec/MultipleExpectations:
  Max: 4
  Exclude:
    - "spec/models/poly/migration_spec.rb"

RSpec/ExampleLength:
  Max: 10
  Exclude:
    - "spec/models/poly/migration_spec.rb"
