plugins:
  - rubocop-rails
  - rubocop-factory_bot
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 3.2
  # Adopt new cops deliberately (run `rubocop` after a toolchain bump and resolve),
  # rather than auto-enabling every pending cop on RuboCop's release schedule —
  # which makes CI non-deterministic when the gem set floats.
  NewCops: disable
  SuggestExtensions: false
  Exclude:
    - "vendor/**/*"

Style/Documentation:
  Enabled: false

Style/ClassAndModuleChildren:
  EnforcedStyle: compact

Metrics/BlockLength:
  Exclude:
    - "spec/**/*"
    - "*.gemspec"
    - "lib/poly/joins.rb"

Metrics/AbcSize:
  Exclude:
    - "lib/poly/joins.rb"

Metrics/MethodLength:
  Exclude:
    - "lib/poly/joins.rb"

Metrics/ParameterLists:
  Max: 6

Rails/RakeEnvironment:
  Enabled: false

RSpec/MultipleExpectations:
  Max: 4
  Exclude:
    - "spec/models/poly/migration_spec.rb"

RSpec/ExampleLength:
  Max: 10
  Exclude:
    - "spec/models/poly/migration_spec.rb"
